sudo: false

language:
  - python

python:
  - 2.7
  - 3.6

install:
  - pip install -r requirements.txt

script:
  - pycodestyle keras-mdn-layer
  - pytest --verbose --capture=no keras-mdn-layer
  - pip install .

notifications:
  email: false